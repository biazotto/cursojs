const pessoas = [
    {
        nome: 'Reginaldo',  
        cargo: 'Programador'
    },
    {
        nome: 'Xumes',  
        cargo: 'Instrutor'
    }
    ];
    // retorna cada uma das pessoas, pois o map interage dentro do array
    // pessoas.map(key => { console.log(pessoas[key]) }
    // retorna um objeto direto
    console.log(pessoas.nome);
    console.log(pessoas.cargo);
    //Ou ainda, desta maneira
    console.log(pessoas[nome])
    console.log(pessoas[cargo])